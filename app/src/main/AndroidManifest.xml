<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.m">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".LoginActivity2"
            android:label="@string/title_activity_login2"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".FooterRepeat" />
        <activity android:name=".HeaderGradient" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PhotoseActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".EventsActivity" />
        <activity android:name=".PlacesActivity" />
        <activity android:name=".MessagesActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".NewsFeedActivity" />
        <activity android:name=".ANdroidDashboardDesignActivity" />
        <activity android:name=".FooterLayout" />
        <activity android:name=".FragmentLayout" />
        <activity android:name=".ActionBar" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>